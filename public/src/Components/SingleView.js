import React from 'react';

export default class SingleView extends React.Component {

    render() {
        return (
            <h1>Here goes the ui - inputs, dropdowns, toggles for single item view/edit</h1>
        );
    }
}